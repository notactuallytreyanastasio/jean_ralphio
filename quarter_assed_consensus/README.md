# Quarter Assed Consensus

This will be where I build a consensus protocol whose rules I make up as I go to get the database/git implementations in each language synchronized with each other like a distributed computer, except bad.
